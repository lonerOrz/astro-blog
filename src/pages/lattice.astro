---
import Layout from "../layouts/Layout.astro";
import Container from "../components/Container.astro";
---

<Layout
  title="Torus Knot"
  description="Minimal mathematical topology sculpture."
>
  <Container>
    <div class="mx-auto max-w-6xl py-10">
        <div
          class="mb-8 flex flex-col gap-6 border-b border-zinc-200 pb-6 md:flex-row md:items-end md:justify-between dark:border-zinc-800"
        >
          <div>
            <div
              class="mb-4 inline-flex items-center gap-2 border border-zinc-300 bg-zinc-100 px-3 py-1.5 dark:border-zinc-800 dark:bg-zinc-950"
            >
              <div class="h-1.5 w-1.5 rounded-full bg-zinc-900 dark:bg-white"></div>
              <span class="text-[9px] tracking-[0.35em] text-zinc-500 uppercase">
                TOPOLOGICAL GEOMETRY
              </span>
            </div>
            <h1
              class="text-3xl font-black tracking-[0.28em] text-zinc-900 uppercase md:text-4xl dark:text-white"
            >
              TORUS // KNOT
            </h1>
            <p
              class="mt-4 max-w-lg text-[10px] leading-relaxed tracking-[0.22em] text-zinc-500 uppercase"
            >
              Minimal parametric topology generated through toroidal harmonic
              motion.
            </p>
          </div>
          <div class="grid grid-cols-2 gap-8">
            <div>
              <div class="text-[8px] tracking-[0.28em] text-zinc-400 uppercase">
                Formula
              </div>
              <div class="mt-1 font-mono text-[11px] tracking-wide text-zinc-700 dark:text-zinc-300">
                TORUS KNOT
              </div>
            </div>
            <div>
              <div class="text-[8px] tracking-[0.28em] text-zinc-400 uppercase">
                Geometry
              </div>
              <div class="mt-1 font-mono text-[11px] tracking-wide text-zinc-700 dark:text-zinc-300">
                PARAMETRIC
              </div>
            </div>
          </div>
        </div>
      <section class="relative overflow-hidden border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-black">
        <div
          class="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(0,0,0,0.03),transparent_72%)] dark:bg-[radial-gradient(circle_at_center,rgba(255,255,255,0.04),transparent_72%)]"
        >
        </div>
        <div
          class="pointer-events-none absolute inset-0 opacity-[0.04] dark:opacity-[0.02]"
          style="
            background-image:
              linear-gradient(rgba(0,0,0,0.06) 1px, transparent 1px),
              linear-gradient(90deg, rgba(0,0,0,0.06) 1px, transparent 1px);
            background-size: 90px 90px;
          "
        >
        </div>
        <div
          class="absolute top-6 left-6 z-10 border border-zinc-200 bg-white/70 px-4 py-3 backdrop-blur-xl dark:border-zinc-800 dark:bg-black/70"
        >
          <div class="text-[8px] tracking-[0.3em] text-zinc-400 uppercase">
            Knot State
          </div>
          <div
            id="formula-readout"
            class="mt-2 font-mono text-[11px] tracking-wide text-zinc-700 dark:text-zinc-300"
          >
            p = 3 / q = 5 / r = 0.32
          </div>
        </div>
        <div class="relative h-[760px] w-full">
          <canvas id="math-canvas" class="h-full w-full"></canvas>
        </div>
        <div
          class="pointer-events-none absolute inset-x-0 bottom-0 h-40 bg-gradient-to-t from-white to-transparent dark:from-black"
        >
        </div>
      </section>
      <section
        class="mt-6 grid gap-6 border border-zinc-200 bg-zinc-50/50 p-6 backdrop-blur-xl dark:border-zinc-800 dark:bg-zinc-950/50 md:grid-cols-3"
      >
        <div>
          <div class="mb-4 flex items-center justify-between">
            <div class="text-[10px] tracking-[0.24em] text-zinc-500 uppercase">
              Rotation
            </div>
            <div id="p-readout" class="font-mono text-[11px] text-zinc-900 dark:text-white">3</div>
          </div>
          <input
            id="input-p"
            type="range"
            min="1"
            max="8"
            step="1"
            value="3"
            class="slider"
          />
        </div>
        <div>
          <div class="mb-4 flex items-center justify-between">
            <div class="text-[10px] tracking-[0.24em] text-zinc-500 uppercase">
              Harmonic
            </div>
            <div id="q-readout" class="font-mono text-[11px] text-zinc-900 dark:text-white">5</div>
          </div>
          <input
            id="input-q"
            type="range"
            min="2"
            max="12"
            step="1"
            value="5"
            class="slider"
          />
        </div>
        <div>
          <div class="mb-4 flex items-center justify-between">
            <div class="text-[10px] tracking-[0.24em] text-zinc-500 uppercase">
              Radius
            </div>
            <div id="r-readout" class="font-mono text-[11px] text-zinc-900 dark:text-white">
              0.32
            </div>
          </div>
          <input
            id="input-r"
            type="range"
            min="0.1"
            max="0.8"
            step="0.01"
            value="0.32"
            class="slider"
          />
        </div>
      </section>
    </div>
  </Container>
</Layout>

<style is:global>
  .slider {
    width: 100%;
    appearance: none;
    height: 1px;
    background: rgb(209 213 219);
    outline: none;
  }
  :global(.dark) .slider {
    background: rgb(63 63 70);
  }
  .slider::-webkit-slider-thumb {
    appearance: none;
    width: 12px;
    height: 12px;
    border-radius: 9999px;
    background: rgb(24 24 27);
    border: 2px solid white;
    cursor: pointer;
  }
  :global(.dark) .slider::-webkit-slider-thumb {
    background: white;
    border-color: black;
  }
  .slider::-moz-range-thumb {
    width: 12px;
    height: 12px;
    border-radius: 9999px;
    background: rgb(24 24 27);
    border: 2px solid white;
    cursor: pointer;
  }
  :global(.dark) .slider::-moz-range-thumb {
    background: white;
    border-color: black;
  }
</style>

<script is:inline>
  const canvas = document.getElementById("math-canvas");
  const ctx = canvas?.getContext("2d", { alpha: false });
  const inputP = document.getElementById("input-p");
  const inputQ = document.getElementById("input-q");
  const inputR = document.getElementById("input-r");
  const pReadout = document.getElementById("p-readout");
  const qReadout = document.getElementById("q-readout");
  const rReadout = document.getElementById("r-readout");
  const formulaReadout = document.getElementById("formula-readout");

  let width = 0,
    height = 0,
    cx = 0,
    cy = 0,
    scale = 0;

  function resize() {
    if (!canvas) return;
    const dpr = Math.min(window.devicePixelRatio || 1, 1.5);
    width = canvas.offsetWidth;
    height = canvas.offsetHeight;
    canvas.width = width * dpr;
    canvas.height = height * dpr;
    ctx?.scale(dpr, dpr);
    cx = width / 2;
    cy = height / 2;
    scale = Math.min(width, height) * 0.14;
  }

  window.addEventListener("resize", resize);
  resize();

  let t = 0;
  let lastTime = performance.now();

  function render(now) {
    if (!canvas || !ctx || !inputP || !inputQ || !inputR) return;
    const deltaTime = (now - lastTime) / 16.6;
    lastTime = now;
    t += 0.002 * deltaTime;

    const density = parseFloat(inputP.value);
    const layers = parseFloat(inputQ.value);
    const amplitude = parseFloat(inputR.value);

    if (pReadout) pReadout.textContent = density.toString();
    if (qReadout) qReadout.textContent = layers.toString();
    if (rReadout) rReadout.textContent = amplitude.toFixed(2);
    if (formulaReadout)
      formulaReadout.textContent = `d = ${density} / l = ${layers} / a = ${amplitude.toFixed(2)}`;

    const isDark = document.documentElement.classList.contains("dark");
    ctx.fillStyle = isDark ? "#000" : "#fff";
    ctx.fillRect(0, 0, width, height);

    const rotY = t * 0.22;
    const rotX = Math.sin(t * 0.4) * 0.25;
    const cosY = Math.cos(rotY),
      sinY = Math.sin(rotY);
    const cosX = Math.cos(rotX),
      sinX = Math.sin(rotX);

    ctx.lineWidth = 0.7;

    for (let z = -layers; z <= layers; z += 0.4) {
      const cosZ = Math.cos(z);
      const sinZ = Math.sin(z);
      const alpha = 0.04 + ((z + layers) / (layers * 2)) * 0.16;
      if (isDark) {
        ctx.strokeStyle = `rgba(255,255,255,${alpha})`;
      } else {
        ctx.strokeStyle = `rgba(0,0,0,${alpha + 0.04})`;
      }
      ctx.beginPath();
      let started = false;
      for (let x = -Math.PI; x <= Math.PI; x += 0.06) {
        const y = Math.sin(x * density + t) * cosZ * amplitude;
        const gyroid =
          Math.sin(x) * Math.cos(y) + Math.sin(y) * cosZ + sinZ * Math.cos(x);
        const px = x * 1.4;
        const py = gyroid;
        const pz = z;
        const rx = px * cosY - pz * sinY;
        const rz = px * sinY + pz * cosY;
        const ry = py * cosX - rz * sinX;
        const finalZ = py * sinX + rz * cosX;
        const p = 1 / (1 + finalZ * 0.18);
        const sx = cx + rx * scale * p;
        const sy = cy + ry * scale * p;
        if (!started) {
          ctx.moveTo(sx, sy);
          started = true;
        } else {
          ctx.lineTo(sx, sy);
        }
      }
      ctx.stroke();
    }
    requestAnimationFrame(render);
  }
  requestAnimationFrame(render);
</script>
