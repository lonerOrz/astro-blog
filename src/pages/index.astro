---
import Layout from "@layouts/Layout.astro";
import Container from "@components/Container.astro";
import { SITE, HOME, SOCIALS } from "@consts";
import Link from "@components/Link.astro";
import { getCollection } from "astro:content";
import type { CollectionEntry } from "astro:content";

const blog = (await getCollection("blog"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, SITE.NUM_POSTS_ON_HOMEPAGE);

const projects: CollectionEntry<"projects">[] = (
  await getCollection("projects")
)
  .filter((project) => !project.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, SITE.NUM_PROJECTS_ON_HOMEPAGE);
---

<Layout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <aside data-pagefind-ignore class="mx-auto max-w-3xl">

      <!-- ── Intro ─────────────────────────────────────── -->
      <section class="animate space-y-8 py-16">
        <p class="font-mono text-[11px] uppercase tracking-[0.15em] text-zinc-400 dark:text-zinc-500">
          {SITE.TITLE}
        </p>
        <h1
          class="text-5xl font-bold tracking-tight text-zinc-900 dark:text-zinc-50 leading-[1.1]"
        >
          Digital garden.<br />Notes and builds.
        </h1>
        <p class="text-base leading-relaxed text-zinc-500 dark:text-zinc-400 max-w-sm">
          A developer who values simplicity and meaningful code. This space
          documents learning, shares insights, and captures fleeting ideas.
        </p>
        <p class="font-mono text-sm text-zinc-400 dark:text-zinc-600">
          今こそ、救いの時だ。
        </p>
      </section>

      <hr class="border-zinc-200 dark:border-zinc-800" />

      <!-- ── Recent Writing ────────────────────────────── -->
      <section class="animate space-y-5 py-14">
        <div class="flex items-center justify-between">
          <h2 class="text-sm font-semibold text-zinc-900 dark:text-zinc-50">
            Recent Writing
          </h2>
          <Link
            href="/blog"
            underline={false}
            class="text-xs text-zinc-400 dark:text-zinc-500 hover:text-zinc-900 dark:hover:text-zinc-50 transition-colors duration-150"
          >
            All articles →
          </Link>
        </div>

        <ul>
          {
            blog.map((post, i) => (
              <li
                class="stagger-item"
                style={`--delay: ${150 + i * 60}ms`}
              >
                <a
                  href={`/blog/${post.id}`}
                  class="btn-interactive group flex items-center gap-4 py-3.5 border-b border-zinc-100 dark:border-zinc-800 hover:bg-zinc-50 dark:hover:bg-zinc-900/60 -mx-2 px-2 rounded-sm"
                >
                  <span class="flex-1 text-sm text-zinc-900 dark:text-zinc-50 truncate group-hover:underline underline-offset-2 decoration-zinc-300 dark:decoration-zinc-600">
                    {post.data.title}
                  </span>
                  <time class="font-mono text-[11px] text-zinc-400 dark:text-zinc-500 shrink-0">
                    {post.data.date.toLocaleDateString("en-US", {
                      month: "short",
                      year: "numeric",
                    })}
                  </time>
                </a>
              </li>
            ))
          }
        </ul>
      </section>

      <!-- ── Projects ──────────────────────────────────── -->
      {
        projects.length > 0 && (
          <Fragment>
            <hr class="border-zinc-200 dark:border-zinc-800" />
            <section class="animate space-y-5 py-14">
              <div class="flex items-center justify-between">
                <h2 class="text-sm font-semibold text-zinc-900 dark:text-zinc-50">
                  Projects
                </h2>
                <Link
                  href="/projects"
                  underline={false}
                  class="text-xs text-zinc-400 dark:text-zinc-500 hover:text-zinc-900 dark:hover:text-zinc-50 transition-colors duration-150"
                >
                  All projects →
                </Link>
              </div>
              <ul>
                {projects.map((project, i) => (
                  <li
                    class="stagger-item"
                    style={`--delay: ${250 + i * 60}ms`}
                  >
                    <a
                      href={`/projects/${project.id}`}
                      class="btn-interactive group flex items-center gap-4 py-3.5 border-b border-zinc-100 dark:border-zinc-800 hover:bg-zinc-50 dark:hover:bg-zinc-900/60 -mx-2 px-2 rounded-sm"
                    >
                      <span class="flex-1 text-sm text-zinc-900 dark:text-zinc-50 truncate group-hover:underline underline-offset-2 decoration-zinc-300 dark:decoration-zinc-600">
                        {project.data.title}
                      </span>
                      <span class="font-mono text-[11px] text-zinc-400 dark:text-zinc-500 shrink-0 hidden sm:block max-w-[180px] truncate text-right">
                        {project.data.description}
                      </span>
                    </a>
                  </li>
                ))}
              </ul>
            </section>
          </Fragment>
        )
      }

      <hr class="border-zinc-200 dark:border-zinc-800" />

      <!-- ── Connect ───────────────────────────────────── -->
      <section class="animate space-y-5 py-14">
        <h2 class="text-sm font-semibold text-zinc-900 dark:text-zinc-50">
          Connect
        </h2>
        <ul class="flex flex-wrap gap-x-6 gap-y-2.5">
          {
            SOCIALS.map((SOCIAL) => (
              <li>
                <Link
                  href={SOCIAL.HREF}
                  external
                  underline={false}
                  class="text-sm text-zinc-500 dark:text-zinc-400 hover:text-zinc-900 dark:hover:text-zinc-50 transition-colors duration-150"
                >
                  {SOCIAL.NAME}
                </Link>
              </li>
            ))
          }
          <li>
            <Link
              href={`mailto:${SITE.EMAIL}`}
              underline={false}
              class="text-sm text-zinc-500 dark:text-zinc-400 hover:text-zinc-900 dark:hover:text-zinc-50 transition-colors duration-150"
            >
              {SITE.EMAIL}
            </Link>
          </li>
        </ul>
      </section>

    </aside>
  </Container>
</Layout>
